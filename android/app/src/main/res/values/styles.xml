<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Android 12+ mandatory splash (compat Theme.SplashScreen).
      Uses branded logo + same background as Flutter AuthenticatedLogoSplash /
      Get Started splash so Pixel does not fall back to the circular launcher icon.
    -->
    <style name="LaunchTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/splash_background</item>
        <item name="postSplashScreenTheme">@style/NormalTheme</item>
        <!-- Pre-12 / window fill while Flutter attaches -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
</resources>
