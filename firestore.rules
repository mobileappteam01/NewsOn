rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;   // TEMPORARY — allows public reads
      allow write: if false; // keep writes blocked
    }
  }
}